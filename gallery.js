// Gallery Functions

// getting the see more button from html
const button = document.querySelector('#see_more');
// Selecting and converting all the images that are hidden

// let hiddenImgaes = Array.from(document.querySelectorAll('.hidden'));

// button.addEventListener('click',(event)=>{
//      hiddenImgaes.map(hiddenImgae => {
//           hiddenImgae.style.display = 'block'
//      })
// })